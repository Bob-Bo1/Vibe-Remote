## v22 诊断收敛与 CABLE 检测修正版（2026-09-06）

- 删除“系统权限”和“检查与修复”两个独立页面，保留“连接与配置”“按键映射”“诊断”三栏。
- “诊断”页只保留运行状态和日志入口；连接、VB-CABLE 和语音软件状态回到对应配置位置显示。
- 修复 VB-CABLE 已安装却长期显示“正在检测”的问题：连接页改为读取诊断控制器实际提供的 `checkId` 字段。
- 删除不再使用的 `PermissionsPage.qml`，更新 QML 加载、构建和说明文档契约。

## v21 连接与配置流程版（2026-09-05）

- 首页面改名为“连接与配置”，按“连接设备 → 准备语音通道 → 选择语音软件 → 完成测试”排列。
- 连接按钮旁显示真实 BLE 状态；桥接进程启动、等待设备和遥控器已连接分开显示。
- 全部按键支持改为连接时自动准备，仅在首次需要时请求一次明确授权，页面提供重试状态。
- VB-CABLE 先检测，缺少时提供安装入口；安装并重启后可回到页面重新检测，方向仍固定为
  CABLE Input（语音输出）和 CABLE Output（识别输入）。
- 新安装默认使用微信电脑端 Ctrl+Win 按住说话；豆包、Windows 听写和自定义快捷键改为可选语音预设。
- 按键映射页沿用 v20 的定位线和可滚动宽弹层修复。
- 本版已完成源码测试与软件渲染验收，实体按键、BLE 连接和语音链路仍需用户在新包上复验。

## v20 按键连线与选择弹层修正版（2026-09-05）

- 连接线提升到遥控器图片上层，线端直接落到实物按键中心，保留卡片与图片双向定位。
- 配置下拉弹层加宽并限制高度，长选项可通过滚轮继续查看；卡片内的紧凑布局保持不变。
- 本版需要重新完成便携包启动、实体按键和微信语音复验，界面自检通过不等于遥控器已完成真机验收。

## v19 按键映射页视觉候选版（2026-09-05）

- 按键映射页改为遥控器居中、左右按键卡片围绕的布局，增加按键热点与卡片之间的定位线。
- 卡片内清晰区分单击、双击、长按，保留快捷键直接输入、录制、真实按键检测、恢复默认和显式保存。
- 只调整 RC003 设置页的呈现方式；原有按键模型、保存校验、设备切换和语音通道逻辑继续复用。
- 本版需要重新完成便携包启动、实体按键和微信语音复验，界面自检通过不等于遥控器已完成真机验收。

## v18 视觉交互候选版（2026-09-05）

- 基于 v17 增加固定侧栏、统一明暗配色、圆角控件和固定保存栏。
- 按键设置清晰区分单击、双击和长按，列表随宽度调整并保留选中项可见。
- 原创应用图标统一到 EXE、窗口与侧栏，提供 9 种 Windows 尺寸。
- 236 项相关测试和打包自检通过；新包实体按键和微信语音待复验。

# Changelog — Remote Mic RC003 (Windows)

内部构建版本号记录在 `pyproject.toml` 的 `version`
（当前 `0.1.0-candidate`），仓库级 tag 只作为发布编号，两者对应关系以每条
发布说明为准。正式版 tag 格式：`v<内部版本>-windows`；候选版为
`v<内部版本>-windows-rc003-candidate.<序号>`。

## [0.1.0] — 2026-07-31

标签：`v0.1.0-windows`（基于 `271ed79`）

正式发布。本版本在真实 RC003 遥控器上完成逐键、语音链路验收（方向/OK/Home/
Menu/TV/Power/返回/音量± 单次触发，麦克风键启动豆包输入法并识别语音），
内容与候选版 `v0.1.0-windows-rc003-candidate.1` 相同。

## [0.1.0-candidate] — 2026-09-04（微信语音修复版）

### 修复

- **微信 Ctrl+Win 按住状态不稳定**：微信专用快捷键改走一次性批量
  `SendInput`，按下和释放保持同一组扫描码事件；豆包等其他快捷键继续使用
  原来的带标记 `keybd_event` 物理化路径。
- **语音诊断记录更准确**：日志会显示当前快捷键发送后端，便于区分微信快捷键、
  遥控器音频和虚拟声卡三个环节。

## [0.1.0-candidate] — 2026-09-03（修复版）

### 公开使用整理

- **完整按键一键配置**：新增 `enable-full-keys.cmd` 和
  `disable-full-keys.cmd`。启用前会明确征得用户同意，只给已验证的 Frida
  运行目录添加 Defender 排除，并写入旁路开关；关闭时恢复默认配置并移除同一条
  固定目录排除。
- **本机实体验收**：在一台真实 Windows 电脑上确认返回键、音量+、音量−的
  Frida HID usage 已收到并完成映射；发布说明保留“不同电脑仍需复测”的边界。
- **便携包路径修复**：ZIP 内部顶层目录改为短名称 `RemoteMicRC003`，降低
  Windows 自带解压器遇到路径过长的概率；ZIP 文件名仍保留版本号。

### v14 修复

- **蓝牙握手期间普通按键失效**：先启动 RC003 HID/Raw Input 按键监听，
  再进行 BLE/ATVV 发现与握手。蓝牙暂时连不上时，返回、音量和导航键仍可工作。

### 修复

- **微信语音文字提交卡顿**：使用 Frida HID tap 的 Mic 释放边沿及时释放
  `Ctrl+Win`，避免等待 250 毫秒安全计时器或迟到的 F5 释放；低层 F5
  专用路径跳过普通按键的关联等待，避免旧会话释放与新会话按下重叠。
- **设置说明更准确**：说明微信电脑版没有麦克风下拉框时，应在 Windows
  的系统“声音→输入”里选择 `CABLE Output`。

## [0.1.0-candidate] — 2026-07-31

标签：`v0.1.0-windows-rc003-candidate.1`（基于 `6c33fcc`）

首个 Windows RC003 候选发布。本版本已在真实 RC003 遥控器上完成逐键、
语音链路验收（详见 README“真机验收”部分）。CI 与自动构建仍然不能
替代真机验证。

### 新增

- **Frida HID tap 旁路**：对 Windows 普通输入链路拿不到的返回、音量+、
  音量- 缺失 usages（`0xF1`、`0x80`、`0x81`），复用上游
  `remote-bridge-hub` 的 Frida Gadget WUDFHost tap 读取；扩展为上报
  遥控器全部键盘 usage，作为所有普通按键的输入旁路。Gadget 是可选的
  第三方二进制，需显式获取（`build/fetch-frida-gadget.ps1`）且验证
  固定 SHA-256 后才会启用。
- **豆包语音触发（DoubaoPhysicalizer）**：注入的右 Alt 合成事件此前被
  豆包输入法 `ImeService` 的低层键盘钩子以 `LLKHF_INJECTED` 标志忽略；
  现在附加到 `ImeService.exe` 的低层回调，只对该标记事件清除 injected /
  lower-integrity 标志并清空 `dwExtraInfo`，使豆包看到的按键形状与
  实体右 Alt 一致。默认按住模式 `ralt`、切换模式 `ralt+space`。
- **设置页独立入口**：`RemoteMicRC003Settings.exe` 与桥接 EXE 分离
  （后合并为单个 EXE，见下）。
- **按键采集/回放工具**：`src/rc003_key_test.py`、`rc003_key_probe.py`
  等诊断工具，被动记录真实物理签名，不执行映射动作。

### 修复

- **普通按键双触发**：方向键、OK 等按键按下时一次动作被触发两次。
  根因是低层键盘钩子阻塞了 `WM_INPUT` 派发，导致“先 arm 后吞键”的
  等待式方案永远慢半拍。改为由 Frida GATT tap 的独立 socket 线程在
  `NtDeviceIoControlFile` 报告到达时 arm，低层钩子零等待匹配并吞掉
  原生按键，只注入一次映射动作。方向/OK/Home/Menu/TV/Power/返回/
  音量键全部实测通过。
- **F5 语音键重复替换刷屏**：按住麦克风键期间键盘 auto-repeat 会让
  “替换为右 Alt”逻辑反复触发；为 transform 增加已按下/已发送守卫，
  只在真实按下/释放边沿各发送一次。
- **BLE GATT 特征找不到**：修复后反复出现
  `ATVV characteristic not found`；改用 `BluetoothCacheMode.UNCACHED`
  读取服务与特征，避免 Windows 缓存旧枚举结果。
- **设置保存失败且映射不生效**：配置文件改为临时文件 + fsync +
  `os.replace` 原子写入；Qt 设置保存捕获一切持久化/回读异常并在界面
  显示错误；桥接进程在按键前按 mtime 热加载新的按键映射，磁盘数据
  损坏时保留最后一份有效映射。
- **启动闪黑色命令行窗口**：桥接启动子进程与打包运行时的控制台子进程
  均使用 `CREATE_NO_WINDOW` 隐藏。
- **语音识别无声/不稳定**：语音输出改为按端点能力输出立体声并复制
  声道；解码后增加 20 Hz 一阶高通 DC 阻挡；默认增益提高到 +10 dB；
  16 kHz → 48 kHz 改有状态连续插值（对齐上游）。实机验收：豆包输入法
  能识别遥控器语音。

### 变更

- **单 EXE 行为**：合并为同一个 `RemoteMicRC003.exe`。双击（无参数）或
  `--settings` 打开设置窗口；`--bridge` 显式启动桥接进程。便携版按这个规则
  启动设置和桥接。
- **设置保存原子化**：`save_config` / `save_key_bindings` 走原子写入，
  不暴露半写的 JSON。
- **返回键默认映射**：保持 `delete_backward`（退格）语义；新增可选的
  “浏览器后退”动作供用户在设置页手动绑定。
- 普通按键仍通过 `SendInput` 注入映射动作；语音快捷键通过物理化的
  右 Alt 事件；两者互不混用。

### 已知限制

- 未签名，首次运行会触发 SmartScreen 提示，属预期行为。
- Frida Gadget 与 VB-CABLE 均为可选第三方组件；未显式获取/安装时，
  缺失 usages 不会被猜测伪造，语音默认没有虚拟麦克风路由。
- 遥控器没有独立物理静音键；“系统静音”只是可选手动绑定。
- 便携版运行期配置写入 `%LOCALAPPDATA%\RemoteMic\RC003`，删除解压目录
  不会自动删除这些配置和日志。
